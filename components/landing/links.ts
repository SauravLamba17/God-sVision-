/* The reference design pointed every call-to-action at the in-page #start
 * anchor, since a static prototype had nowhere else to go. In the real app
 * they go to the auth page — Sign in to its Sign In tab, Get started to its
 * Register tab. */
export const SIGNIN_HREF = '/auth/signin'
export const REGISTER_HREF = '/auth/signin?tab=register'
